**Largest Element in the Array**
**Given an "ARR" of size "N". Find the Largest Element in the array.**
**Eg-**

**Input- N = 5 , Arr = {1,2,3,4,5}**

**Output- 5**

#include <bits/stdc++.h> 
int largestElement(vector<int> &arr, int n) {
    int largest = arr[0];
    for(int i=1; i<n; i++)
    {
        if (arr[i]>largest)
        {
            largest = arr[i];
        }
    }
    return largest;
}
